name: panorama_backend
description: REST API (/api/v1) для «Шахматки квартир» поверх PostgreSQL.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  postgres: ^3.5.0
  crypto: ^3.0.3
  googleapis_auth: ^1.6.0
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
